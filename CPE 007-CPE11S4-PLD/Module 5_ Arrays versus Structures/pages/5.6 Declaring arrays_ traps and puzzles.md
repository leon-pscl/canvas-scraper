Take a look at the following declaration →

In this way, we’ve declared a variable array which is a 10-element array of pointers to data of type int.

And now let's look at a seemingly very similar, but completely different, declaration →

It declares array as a pointer to a 10-element array of type int.

Look how the parentheses have changed the meaning of the declaration.

And now something really difficult →

The statement creates a variable array, which is a pointer to a 10-element array whose elements are pointers to ints.

Analyze all three statements yourself and try to identify the rule that determines the proper interpretation of the declaration. We promise we won’t leave you alone with this problem. We’ll spend more time discussing this and even more complicated pointer declarations later.